name: Deploy dev to AWS

#turn off deploy to AWS
on:
  pull_request:
    types: [closed]
    branches:
      - dev

jobs:
  aws-deploy:
    runs-on: ubuntu-latest
    env:
      VITE_APP_CLIENT_ID: ${{secrets.VITE_APP_CLIENT_ID}}
      VITE_APP_API_URL: ${{secrets.VITE_APP_API_URL}}
      VITE_APP_CLIENT_URL: ${{secrets.VITE_APP_CLIENT_URL}}
    steps:
      - uses: actions/checkout@v2
        with:
          ref: dev
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: |
          touch .env
          echo VITE_APP_CLIENT_ID="$VITE_APP_CLIENT_ID" >> .env
          echo VITE_APP_API_URL="$VITE_APP_API_URL" >> .env
          echo VITE_APP_CLIENT_URL=tutorial.eaglesoftwareteam.com >> .env
          echo VITE_APP_SITE_URL=tutorial.eaglesoftwareteam.com >> .env

      name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20.x # or 16 depending on your project

      - name: Install dependencies
        run: npm install

      - name: Install dependencies for vite
        run: npm ci

      - name: Fix vite permissions
        run: chmod +x node_modules/.bin/vite

      - name: Build Vue project
        run: npx vite build

      - name: List build output
        run: ls -R dist

      - name: Deploy to Server
        uses: easingthemes/ssh-deploy@v2.1.1
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
          ARGS: "-rltgoDzvO --delete"
          SOURCE: "dist/"
          REMOTE_HOST: "project1.eaglesoftwareteam.com"
          REMOTE_USER: ubuntu
          TARGET: "/var/www/html/seiv2025/p2/t4"

